package com.motonav.rider

import android.app.Application

/**
 * TODO: this is where Mapbox gets initialized once the SDK is wired in
 * (MapboxNavigationApp.setup(...)), and where the foreground service for
 * background GPS + BLE will eventually live. Left empty for the PoC.
 */
class MotoNavApp : Application()
